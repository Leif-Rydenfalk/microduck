# Eye-ring evidence correction — 2026-09-08

Applied only after root review of the typed withdrawal tool and its negative tests. No physical operation occurred.

The original eight ledger rows remain an exact byte prefix. CLI withdrawals of exact rows 8, 2, and 1 appended rows 9–11, with a hash-pinned read-only artifact audit and explicit reasons. Generated trust moved from stored T2 (stale; prior labels computed T3) through T2 → T1 → T1. Row 8 is licence-position prose, not field-test evidence; row 2 is digital mesh measurement, not physical bench evidence; row 1 references vendor geometry deliberately removed from the part. Historical rows remain auditable CANNOT DETERMINE findings. This correction makes no legal conclusion and no original-part identity claim.

The ordinary evidence CLI appended row 12 with the current hash of the same head-width verdict artifact. The recovered historical version differs only in generated timestamp and plus/minus typography; the numerical results and CANNOT DETERMINE verdict remain unchanged.

Full triad check is **CANNOT DETERMINE**, with four historical CD findings and zero FAIL findings. The original geometry hashes are unchanged; neither vendor geometry nor a phantom identity path was created. Current T1 is the retained digital reference-comparison evidence, not physical verification or confirmed 1:1 original identity.

Validation: nine isolated withdrawal tests PASS, including stale/self/forward/wrong-hash rejection, negative-evidence preservation, generic retraction bypass refusal, and withdrawn artifact authority. The direct-reader test covers 729 three-row ledgers and every eligible positive target; computed trust never increases and unresolved FAIL count never decreases. Existing triad runner: 37 tests PASS. Git whitespace check passes.

Evidence: `proposal.json` retains the pre-application plan and exact target hashes; `ledger-before.jsonl` and `trust-before.json` preserve starting bytes; `application.json` records CLI output, stepwise tiers, prefix and geometry checks; `check-before.json` and `check-after.json` are actual full CLI results. `core.patch` is the core tracked-file diff; the new test lives at workshop `tests/test_positive_withdrawal.py`.
