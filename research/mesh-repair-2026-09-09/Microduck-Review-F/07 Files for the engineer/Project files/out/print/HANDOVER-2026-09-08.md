# HANDOVER — 2026-09-08, printing microduck on the Black Ark H2D

*Written at Leif's request before this Claude instance is reset. Part 1 is his words, verbatim and in order. Part 2 is what was done and where things stand. Part 3 is what is still open.*

## 1 · Leif, verbatim, in order

1. "Alright print the microduck componetns on the 3d printers"
2. "There aws something wrong with the adhesion settings the first few prints. start with printing parts of the head not hte body we start wioth the head look at the recent docs about what we need to biyut and stuff and follow up on that commit and puis everything"
3. "use the h2d printer."
4. "mark the x1 carbon printer as under maintanence for future users of the printfarm. somehow in the system"
5. "if youre able to print more of the body do that also on the same plate as much as possible preferable from the head going down so we can actually build more and more connceted together."
6. "I will cancel the print. make it way more compact. remember no brim."
7. "Alright The plate stopped printing for some reson. print it again. open it in bambu studiuo"
8. "look at the most recently exported version of the duck head and print that it was configured by a engineer here to work perfectly on this printer. send the file using the lan code api."
9. "Alright so weve looked at your 3d mpdels and somehow layer generation fails because the models are wierd. you have to build a validator to validate the resukts before sending to printer because currently its fucked up and it ruined the nossle on one of the printers because of your wierd generation and wierd settings. sometimes layers are missing and somnetimes theyu are too close. you must validate and make sure. every single time. print the whole gead."
10. "fix your 3d models because currently theyre fucked uop"
11. "until youre 100% certain dont send anything else to the printers. fix your workflow for 3d printing and standardize it"
12. "send the head to the h2d. let me look at it in bambu studio. is it coherent. does it have any of the issues i mentioned before with gaps in layers and invalid layers because of the fucked 2d mesh? and also the layout was not 100% optimal with the orientations of some parts being off"
13. "look at the issue i mentioned with the mesh. have you looked at it?"
14. "have you fixed the defects? when 3d rendering it with pbr i also noticed it looked off so maybe its the same bug class"
15. "No it will fail, have you even seen what bambustudio spits out? it tells you the issues. i just took a screenshot which shows the errors. you havce to figure this out"
16. "alright create a list of all my words verbatim and what youre doing i will reset this claude instance since youre starting to loose it"

## 2 · What was done, and the state right now

**Printer.** Only one machine is reachable: "H2D AMS HT", serial 0947BJ610900152, 192.168.1.14, Black Ark, Shenzhen. It has TWO nozzles: **left 0.4 HS01 (MQTT nozzle id 1), right 0.6 HS01 (id 0)**, both Standard flow; the single AMS feeds the left extruder. A file must declare nozzle_diameter [0.4, 0.6] Standard or the printer pauses at 0 % with 05FE_8053 ("left nozzle not matched"). AMS trays: 0 grey, 1 orange, 2 black (36 %, RFID), 3 white (loaded 2026-09-08). The X1 Carbon is disabled in the farm with an UNDER MAINTENANCE note. **The printer is currently STOPPED, bed state unknown (last send was cancelled at 0 %).**

**Farm (ce-print-scheduler, localhost:8770).** OrcaSlicer 2.4.2 installed (BambuStudio's CLI refuses the H2D). New: `slice_overrides.nozzle`, `preferred_tray`, and THE GATE — `scheduler/validate.py` runs on every `bambu.send()` against the live machine and refuses a FAIL (proved: the file that wrecked a nozzle is refused with the layer numbers). SAFE settings forced on every plate: `independent_support_layer_height=0`, `detect_thin_wall=1`, no brim. Tools: `meshfix.py` (weld, drop collapsed faces, consistent winding, outward, admesh, +0.02 mm nudge off slice planes), `meshdoctor.py` (census), `meshcheck.py` (quick), `partgate.py` (each part sliced alone, slicer checks ON). Standard: `ce-print-scheduler/docs/PRINT-WORKFLOW.md`. All committed in ce-workshop (push blocked by GitHub's pre-receive hook, branch 151 commits ahead — pre-existing).

**What was wrong (measured).** (a) Plates sliced with normal supports had 785 layers for a 232-layer object, Z stepping 0.40/0.44/0.45/0.47 — "too close" — that plate ruined the nozzle. (b) Vertex rings and flat faces exactly on slicing mid-planes (eye-ring at 9.30/9.50) → dropped layer + sliver in the air; sub-nozzle fins in vendor meshes → empty layers; sliced with `--no-check` this printed into air (0C00_803F, twice). (c) Mesh defects: vendor meshes with open boundary edges, collapsed faces, wrong winding (the PBR artefacts); our ce-cad exports with sliver faces; one inside-out mesh that was MY flip bug (fixed). Fixed meshes: `out/print/stl/fixed/PLA` (26/26 pass partgate; census clean except 1 non-manifold edge per upper leg from the ce-cad export).

**Plates.** `out/print/plates/HEAD-V5/` — the whole head, 11 pieces, 8 h 47 m, 142 g, no brim, tree supports, left 0.4 nozzle, validate PASS on every check (`VALIDATE.txt`). It was sent once at Leif's word (#12), then STOPPED at 0 % after #15. Nothing printed. Farm job-0001 still points at it.

**Bambu Studio's two messages (#15), what I found.** Screenshot shows the GUI with printer preset 0.4/0.4 and the STOCK process "0.20mm Standard @BBL H2D" (supports OFF, thin wall OFF), and: "eye-ring has an empty initial layer" + "m12-lens-holder has floating regions — enable support". Headless reproduction: Bambu Studio's own engine slices the exported project AS-IS cleanly (rc 0, tree supports, 24 890 s); each of eye-ring / lens holder / neck-pitch alone slices cleanly under stock, thin-wall-only and supports-only; the whole plate under STOCK presets FAILS (-100, internal "ZFiller" error). The GUI's messages were NOT reproduced by the CLI in any configuration; every object sits at z = 0 in the project. Best reading: the GUI re-sliced with stock presets and its per-object validation ran on that. NOT proven. Leif says the plate will fail; **treat HEAD v5 as unsent until the two GUI messages are understood in the GUI itself** (open `plates/HEAD-V5/microduck-head-v5.gcode.3mf`, press Slice, read the messages, jump to the object).

**Docs, committed and pushed (microduck repo).** `out/print/PRINT-H2D.md` (the whole story), `BUILD-PACK.md` §6 (nothing bought; servo decision open), `tools/compact_plate.py` (packer), `out/print/plates/{H2D-COMPACT,HEAD-V4,HEAD-V5}`.

## 3 · Open, in priority order

1. The two Bambu Studio GUI messages on HEAD v5 (eye-ring empty initial layer, lens-holder floating regions): reproduce IN the GUI with the project's own settings, not stock; if real, fix the eye-ring's bottom and the lens holder's pose, re-run the full workflow, validate, then send. Do not send before.
2. Body plates: trunk, hips, legs, feet from `stl/fixed/PLA` through the same workflow. banana-pcb-locker and bearing-roll were dropped once for empty layers; they pass partgate after meshfix — re-check.
3. TPU parts need TPU on the external spool.
4. ce-cad export leaves sliver/non-manifold faces on our own parts (upper legs): fix in the exporter.
5. ce-workshop push is rejected by GitHub (oversized history); the farm commits are local only.
6. Buying: nothing bought; XL330 vs Feetech STS3215 decision still Leif's (`out/procurement/SOURCING-CN.md`).
