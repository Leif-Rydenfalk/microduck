# RFQ source corrections — 2026-09-08

Corrected the owning `spec/sourcing.json` questions for camera, fasteners, Radxa CSI and ROBOTIS power/custom leads. No supplier was contacted. No price, quantity, offer selection, supplier contact or unrelated record changed; the regression restores only the authorized prose fields and proves the entire JSON equals the original afterward.

The camera request explicitly rejects Seeed102110719 for the current24x25mm-class requirement; its documented38x38x16mm module is not an accepted cheaper quote. CSI cut length remains unknown. Historical15mm proxy arithmetic is not an FFC manufacturing requirement. The16.94mm mesh lens envelope is a comparison dimension, not a proven aperture tolerance. Exact board/connector/lens revisions, active-crop FOV and included regional cable packages are requested.

The fastener request retains historical quote quantities only as conditional enquiries. It records all52 withdrawn servo ISO length proposals and preserves the12 remaining generic runs' unresolved status. ROBOTIS M2 tapping screws and3mm maximum pilot-hole depth are distinguished from metric ISO screws and under-head screw length. No replacement screw list or final per-joint quantities are inferred. ROBOTIS power questions now describe the published runtime voltage conflict rather than claiming the unmeasured pack/servo voltage; modeled lead lengths are no longer requested as production cuts.

The single B8 offer note that said “20x our15mm cut length” was corrected too, because it contradicted the same line's existing route uncertainty. Historical catalog length observations remain recorded.

`sourcing-before.json` preserves the exact pre-edit source bytes; `superseded-rfq-records.json` extracts the four original supplier records for review. `provenance.json` records the source hashes and evidence paths. Original Unicode serialization is preserved. The ordinary generator entry point also patches RELEASE and a BOM, so only its existing SOURCING/RFQ constants were written to their owning pages. `generation.json` records this narrow generation and the unchanged captured RELEASE hash. Its initial BOM check named the wrong path; that null entry is withdrawn. The actual `out/release/bom.csv` was subsequently compared byte-for-byte with Git HEAD and matches; no historical pre-generation BOM hash is claimed. Root retains readiness and live fastener outputs.

Validation: **28 sourcing tests PASS**, including five new RFQ tests. Negative controls reproduce detection of the original rejected-camera request and both proxy-cut requests, and reject a newly appended “we need ~15mm” clause even alongside a correct disclaimer. Scoped whitespace check passes. These are document/source checks, not original identity, procurement approval or physical acceptance.
