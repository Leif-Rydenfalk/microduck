export const meta = {
  name: 'microduck-electronics',
  description: 'Electronics lane: datasheets onto the shelf, the robot netlist checked, the Robot HAT re-created as a ce-pcb board with DRC and gerbers',
  phases: [
    { title: 'Datasheets', detail: 'every electronic part on the shelf with its datasheet cited' },
    { title: 'Netlist+PCB', detail: 'robot netlist checked; Robot HAT schematic -> board -> DRC -> gerbers' },
    { title: 'Verify', detail: 'independent checks' },
  ],
}
const REPO = '/Users/leifrydenfalk/dev/ce-workshop/ce-designs/microduck'
const WS = '/Users/leifrydenfalk/dev/ce-workshop'
const CTX = `Repo ${REPO} (git; do NOT commit). GOAL.md rung 4. Read docs/ELECTRONICS-AND-SOFTWARE.md and docs/PARTS.md first — they carry every published fact with file:line sources (servo bus /dev/ttyS2 1 Mbps Protocol 2, IDs, imu_to_dxl v2 LSM6DSV16X as bus ID 200, Robot HAT: TLV320AIC3104 @0x18 on I2C3 (header pins 3/5), dormant BMI088, VL53L5CX/L8CX via Stemma J5 @0x29, half-duplex TTL transceiver, battery-to-board power, FUSB302 disabled; IMX219 on CSI; NP-F550 2S 6.6-8.2 V). Tools: ${WS}/ce-cad skills .claude/skills/elec-datasheets, pcb-design, cad-electrical; cecad.netlist (docs/netlist.md), cecad.pcb/pcbcheck/pcbroute/pcbfab (bin/pcb, docs/pcb.md), ce-parts SCHEMA (${WS}/ce-parts/SCHEMA.md, electrical.<kind>.json records), ce-elec (${WS}/ce-elec/bin/elec). export CE_TRIAD_ROOT="${REPO}:${WS}"; use export CAD_MAX_JOBS=2 and run one build at a time (the Mac is shared). House rules: every figure VERBATIM from the datasheet with quote+cite; unknown stays CANNOT DETERMINE; three verdicts; break a check on purpose before trusting it.`
const OUT = { type: 'object', properties: { files: { type: 'array', items: { type: 'string' } }, verdicts: { type: 'array', items: { type: 'string' } }, cannot_determine: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } }, required: ['files', 'verdicts', 'notes'] }
const VERDICT = { type: 'object', properties: { ok: { type: 'boolean' }, measured: { type: 'array', items: { type: 'string' } }, problems: { type: 'array', items: { type: 'string' } } }, required: ['ok', 'measured', 'problems'] }
phase('Datasheets')
const parts = [
  { key: 'servo-imu', list: 'XL330-M288-T (ROBOTIS e-manual: electrical ratings, control table registers 124..146 used by robotd, connector/cable JST 3-pin X3P, dimensions drawing) and LSM6DSV16X (ST datasheet: SFLP, ranges, I2C/SPI, package) as ce-parts records; check ${WS}/ce-parts first and extend rather than duplicate' },
  { key: 'compute-sensors', list: 'Radxa Zero 3W (wiki: RK3566, 40-pin header pinout incl. UART2_M0 on ttyS2 pins, I2C3 pins 3/5, CSI, USB-C, power), IMX219 sensor module + M12 lens (wide angle), VL53L5CX/VL53L8CX (ST: I2C 0x29, 8x8, 15 Hz), TLV320AIC3104 (TI: I2C 0x18, I2S, MCLK 12 MHz), FUSB302, BMI088' },
  { key: 'power-audio', list: 'NP-F550 pack (2S Li-ion 2600 mAh — find a cell/pack datasheet and the contact pinout), a 2S Li-ion protection/charger reference, a 35x25x7 mm speaker (CANNOT DETERMINE the exact part — pick a representative with datasheet and say so), NFC antenna/IC candidates (say CANNOT DETERMINE for the fitted one)' },
]
const ds = await parallel(parts.map(p => () => agent(`${CTX}\n\nDATASHEETS ONTO THE SHELF (elec-datasheets skill, read it first): ${p.list}. For each: locate the vendor PDF/page online (WebSearch/WebFetch), verify the identity against the part fitted (source cites in docs/ELECTRONICS-AND-SOFTWARE.md), write/extend ce-parts/<slug>/ in ${REPO} (or reference the workshop shelf part if it already exists) with electrical.<kind>.json figures VERBATIM quoted+cited, docs/ with the datasheet URL and fetch date, component.json origin vendor, evidence ledger row kind standard where a standard applies. bin/triad check each ref. Return files, verdicts, CANNOT DETERMINE list.`, { label: `datasheets:${p.key}`, phase: 'Datasheets', schema: OUT })))
const dsr = ds.filter(Boolean)
log(`datasheets: ${dsr.length}/3 lanes`)
phase('Netlist+PCB')
const nets = await agent(`${CTX}\n\nShelf state from the datasheet lanes: ${JSON.stringify(dsr).slice(0, 4000)}\n\nTHE ROBOT NETLIST. Write ${REPO}/electronics/netlist.py: a cecad.netlist.Design of the whole robot from the published facts — host Radxa Zero 3W (add a host roster record if cecad/data/hosts.json or the shelf lacks it, cited from the Radxa wiki pinout), the Robot HAT as a board with its I2C3 devices (codec 0x18, ToF 0x29, BMI088 0x19/0x68 — a duplicate-address check must PASS or name the conflict), the UART2 half-duplex bus with 15 XL330 + imu_to_dxl (bus IDs 10-14,20-24,30-34,200), CSI camera, battery rails (2S 6.6-8.2 V to servos; 5 V to the Radxa via the HAT). Run its check() and record the three-verdict report to electronics/netlist-report.md — CANNOT DETERMINE where the HAT schematic is unpublished (regulator, transceiver). Then run ${WS}/ce-elec/bin/elec if it applies. Return files + verdicts.`, { label: 'netlist', phase: 'Netlist+PCB', schema: OUT })
const pcb = await agent(`${CTX}\n\nNetlist lane: ${JSON.stringify(nets).slice(0, 3000)}\n\nRE-CREATE THE ROBOT HAT AS A ce-pcb BOARD (pcb-design skill; ${WS}/ce-cad/docs/pcb.md; worked example ${WS}/ce-cad/examples/sensor_board/board.py or ce-designs/sensor_board). Board: 65 x 30 mm Pi-Zero footprint (mounting holes at the Pi Zero positions: 3.5 mm from the edges, 58 x 23 mm pattern — cite), 40-pin header footprint, TLV320AIC3104 (TQFP-48 or the fitted package — footprint from IPC-7351B via cecad.pcb with the package dims cited, CANNOT DETERMINE if the datasheet drawing is not read), VL53L5CX Stemma/Qwiic JST-SH 4-pin (J5), a half-duplex TTL transceiver for the Dynamixel bus (pick a documented reference circuit — e.g. 74HC126/74LVC1G125 direction-less buffer as ROBOTIS' own TTL schematic, cite), a 3-pin JST X3P bus connector, battery input + a 5 V buck (a documented reference), speaker + mic connectors, BMI088 (dormant, footprint only). Nets FROM the netlist (bind()), never retyped. Place, pour GND, autoroute, check() -> DRC, plot top/bottom SVG, fab() gerbers, publish_pcb. Write ${REPO}/electronics/robot-hat/board.py + out/ + README.md stating clearly this is OUR reconstruction from the published facts, not Pollen's board. Return files + DRC verdict + CANNOT DETERMINE list.`, { label: 'robot-hat-pcb', phase: 'Netlist+PCB', schema: OUT })
phase('Verify')
const v = await agent(`${CTX}\n\nINDEPENDENTLY VERIFY: datasheet lanes ${JSON.stringify(dsr).slice(0, 2500)}; netlist ${JSON.stringify(nets).slice(0, 2000)}; pcb ${JSON.stringify(pcb).slice(0, 2000)}. Re-run bin/triad check on every ref they wrote; open 5 random figures and confirm each is quoted verbatim from its cited page (WebFetch the URL); run electronics/netlist.py and the board's check() yourself; open the board SVG plot with the Read tool. Return ok + problems file:line.`, { label: 'verify', phase: 'Verify', schema: VERDICT, effort: 'medium' })
return { datasheets: dsr, netlist: nets, pcb, verify: v }
